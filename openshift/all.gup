#!bash -eu
set -x
gup --always
build_type=_build.prod
gup -u "../$build_type/all"

rm -rf "./app"
../install.sh "$build_type" ./app

# remove client binary
rm -r app/bin/passe

# copy dependencies
gup app/lib
