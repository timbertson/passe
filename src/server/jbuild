; vim: set syntax=lisp commentstring=;\ %s:

(jbuild_version 1)

(library (
	(name passe_server)
	(flags (:include ../flags))
	(libraries (
		extlib
		passe
		passe_unix
		hex
		mirage-fs-unix
		mirage-clock-unix
		safepass
		mirage-types
		ptime
	))

	(preprocess (pps (lwt.ppx)))
))
