#!bash
set -eu
dest="$1"
gup --always
cd ..
installfile=passe-server.install
gup -u "$installfile"
opam-installer "$installfile" --prefix="$dest.full"
mv "$dest.full/share/passe-server" "$dest"
rm -r "$dest.full"
