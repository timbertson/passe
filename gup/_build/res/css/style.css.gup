#!bash -eu
. "$(dirname "${BASH_SOURCE[0]}")"/../../build_type.sh
input="../../../src/css/style.less"
gup -u ../bootstrap ../lessc
gup -u ../bootstrap/package.json "$input"
args=""
if [ "$build_type" != "dev" ]; then
	args="$args --clean-css"
fi
../lessc "$args" "$input" "$1"
