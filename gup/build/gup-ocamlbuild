#!bash -eu
if [ "${GUP_XTRACE:-}" -eq 1 ]; then
	set -x
fi
gup --always
BUILDDIR="$PWD"

pushd "../src" >/dev/null
ocamlbuild -build-dir "$BUILDDIR" -use-ocamlfind "$2" -I common
popd >/dev/null

touch "$2"
